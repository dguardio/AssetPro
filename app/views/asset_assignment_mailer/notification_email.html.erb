<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <div class="content">
      <h2>New Asset Assignment</h2>
      
      <p>Hello <%= @recipient.full_name %>,</p>
      
      <div class="card" style="background: var(--light); padding: 16px; margin: 16px 0;">
        <h3 style="margin-top: 0;">Asset Details</h3>
        <p><strong>Name:</strong> <%= @assignment.asset.name %></p>
        <p><strong>Assigned By:</strong> <%= @assignment.assigned_by.full_name %></p>
        <p><strong>Date:</strong> <%= @assignment.created_at.strftime("%B %d, %Y") %></p>
        
        <% if @assignment.asset.description.present? %>
          <p><strong>Description:</strong> <%= @assignment.asset.description %></p>
        <% end %>
      </div>

      <%= link_to "View Asset Details", asset_url(@assignment.asset), class: "btn" %>
      
      <p style="color: var(--secondary); font-size: 0.875rem; margin-top: 24px;">
        If you did not expect this assignment, please contact your system administrator.
      </p>
    </div>
  </body>
</html> 